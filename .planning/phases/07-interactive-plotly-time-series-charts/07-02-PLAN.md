---
phase: 07-interactive-plotly-time-series-charts
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/pxts/plots.py
autonomous: true
requirements:
  - PLT7-01
  - PLT7-02
  - PLT7-07

must_haves:
  truths:
    - "tsplot Plotly figures have range selector buttons (1M/3M/6M/YTD/1Y/All)"
    - "tsplot Plotly figures have a rangeslider visible by default"
    - "tsplot(..., rangeslider=False) produces a figure without the rangeslider"
    - "tsplot_dual Plotly figures have range selector buttons and rangeslider by default"
    - "tsplot_dual left_label/right_label produce colored axis title text"
    - "tsplot and tsplot_dual accept theme='light' and theme='dark' without error"
    - "Matplotlib backend is unaffected by all new parameters"
  artifacts:
    - path: "src/pxts/plots.py"
      provides: "Updated public signatures + Plotly range nav + dual-axis labels + theme routing"
      contains: "rangeslider"
  key_links:
    - from: "tsplot() / tsplot_dual()"
      to: "_plot_ts_plotly() / _plot_ts_dual_plotly()"
      via: "rangeslider and theme forwarded as explicit params"
      pattern: "rangeslider"
    - from: "_plot_ts_plotly"
      to: "xaxis_cfg with rangeselector and rangeslider keys"
      via: "dict extended before fig.update_layout"
      pattern: "rangeselector"
    - from: "_plot_ts_dual_plotly"
      to: "fig.update_yaxes(title_text=left_label, title_font=dict(color=LEFT_COLOR))"
      via: "left_label/right_label forwarded from tsplot_dual"
      pattern: "title_text"
---

<objective>
Add interactive range navigation (range selector buttons + rangeslider) to both Plotly renderers,
add left_label/right_label axis title parameters to tsplot_dual, and wire the theme parameter
into both renderers.

Purpose: Core interactivity upgrades for the Plotly backend. Range navigation makes charts useful
for exploring long time series. Dual-axis labels give visual identity with colored axis titles.

Output: Updated src/pxts/plots.py with new public parameters and Plotly renderer logic.
</objective>

<execution_context>
@C:/Users/adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-interactive-plotly-time-series-charts/07-CONTEXT.md
@.planning/phases/07-interactive-plotly-time-series-charts/07-01-SUMMARY.md

Current signatures extracted from src/pxts/plots.py:

tsplot (line 715):
    def tsplot(df, cols=None, title="", subtitle="", labels=False,
               hlines=None, vlines=None, date_format=None,
               ylim=None, xlim=None, backend=None, **kwargs):

tsplot_dual (line 763):
    def tsplot_dual(df, left, right, title="", subtitle="", labels=False,
                    hlines=None, vlines=None, date_format=None,
                    ylim_lhs=None, ylim_rhs=None, xlim=None,
                    backend=None, **kwargs):

_plot_ts_plotly (line 435):
    def _plot_ts_plotly(df, cols, title, subtitle, labels, hlines, vlines,
                        date_format, ylim=None, xlim=None, **kwargs):
        # builds xaxis_cfg, creates fig, calls fig.update_layout(template="pxts", xaxis=xaxis_cfg)

_plot_ts_dual_plotly (line 631):
    def _plot_ts_dual_plotly(df, left, right, ...):
        # existing: fig.update_yaxes(tickfont=dict(color=LEFT_COLOR), secondary_y=False)
        # existing: fig.update_yaxes(tickfont=dict(color=RIGHT_COLOR), secondary_y=True)

Color constants (line 29-30):
    LEFT_COLOR = "#0072B2"   # Blue
    RIGHT_COLOR = "#E69F00"  # Orange

Dark theme constants from Plan 01 (theme.py):
    DARK_BACKGROUND_COLOR = "#1a1a2e"
    DARK_PLOT_COLOR = "#16213e"
    DARK_GRID_COLOR = "#2d2d5a"
    DARK_FONT_COLOR = "#e0e0e0"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Range nav in _plot_ts_plotly and updated tsplot signature</name>
  <files>src/pxts/plots.py</files>
  <action>
0. Create the verify script at tests/_verify_07_02.py with the following content (use the Write
   tool or open(..., 'w') in a short Python snippet — the file MUST exist before the verify
   command runs):

```python
# tests/_verify_07_02.py
import pandas as pd
import numpy as np
from pxts.plots import tsplot
from pxts.theme import DARK_BACKGROUND_COLOR
import matplotlib
matplotlib.use("Agg")

idx = pd.date_range("2024-01-01", periods=5, freq="D")
df = pd.DataFrame({"A": [1.0, 2.0, 3.0, 2.5, 1.5], "B": [5.0, 4.0, 3.0, 4.5, 5.5]}, index=idx)

# Range selector
fig = tsplot(df, backend="plotly")
rs = fig.layout.xaxis.rangeselector
assert rs is not None, "rangeselector missing"
assert len(rs.buttons) == 6, f"expected 6 buttons, got {len(rs.buttons)}"
labels = [b.label for b in rs.buttons]
for lbl in ["1M", "3M", "6M", "YTD", "1Y", "All"]:
    assert lbl in labels, f"button {lbl!r} missing"

# Rangeslider default on
assert fig.layout.xaxis.rangeslider.visible is True, "rangeslider should be visible by default"

# Rangeslider opt-out
fig2 = tsplot(df, rangeslider=False, backend="plotly")
assert fig2.layout.xaxis.rangeslider.visible is False, "rangeslider should be hidden"

# Dark theme
fig3 = tsplot(df, theme="dark", backend="plotly")
assert fig3.layout.paper_bgcolor == DARK_BACKGROUND_COLOR, (
    f"dark paper_bgcolor: {fig3.layout.paper_bgcolor}"
)

# Matplotlib no error
import matplotlib.figure
fig4 = tsplot(df, rangeslider=False, backend="matplotlib")
assert isinstance(fig4, matplotlib.figure.Figure)
import matplotlib.pyplot as plt
plt.close(fig4)

print("_verify_07_02 OK")
```

1. Update tsplot() public signature — add three params before backend=None:
       annotations=None, rangeslider: bool = True, theme: str = "light"

   Add to docstring Args:
       annotations: list of dicts with keys "x" (date) and "text" (str).
           y auto-looked up from nearest data point. Plotly backend only; ignored on matplotlib.
       rangeslider: show rangeslider below chart. Default True. Plotly backend only.
       theme: "light" (default, white background) or "dark" (dark navy). Plotly backend only.

2. Update tsplot() plotly dispatch call to forward new params (matplotlib call UNCHANGED):
       return _plot_ts_plotly(..., annotations=annotations, rangeslider=rangeslider, theme=theme, **kwargs)

3. Update _plot_ts_plotly signature to accept:
       annotations=None, rangeslider=True, theme="light"

4. Add range selector and rangeslider in _plot_ts_plotly. After xaxis_cfg is built (the
   if/else block that sets tickformatstops or tickformat), extend it BEFORE fig.update_layout:

       xaxis_cfg["rangeselector"] = dict(
           buttons=list([
               dict(count=1,  label="1M",  step="month", stepmode="backward"),
               dict(count=3,  label="3M",  step="month", stepmode="backward"),
               dict(count=6,  label="6M",  step="month", stepmode="backward"),
               dict(count=1,  label="YTD", step="year",  stepmode="todate"),
               dict(count=1,  label="1Y",  step="year",  stepmode="backward"),
               dict(step="all", label="All"),
           ]),
           bgcolor="rgba(255,255,255,0.8)",
           activecolor="#0072B2",
           x=0, y=1.02, xanchor="left", yanchor="bottom",
       )
       xaxis_cfg["rangeslider"] = dict(visible=rangeslider)

5. Apply dark theme in _plot_ts_plotly after fig.update_layout(**layout_kwargs):

       if theme == "dark":
           from pxts.theme import (DARK_BACKGROUND_COLOR, DARK_PLOT_COLOR,
                                    DARK_GRID_COLOR, DARK_FONT_COLOR)
           fig.update_layout(paper_bgcolor=DARK_BACKGROUND_COLOR,
                             plot_bgcolor=DARK_PLOT_COLOR,
                             font=dict(color=DARK_FONT_COLOR))
           fig.update_xaxes(gridcolor=DARK_GRID_COLOR)
           fig.update_yaxes(gridcolor=DARK_GRID_COLOR)

The annotations parameter is accepted in the signature but is a no-op here. Plan 03 adds processing.
  </action>
  <verify>
    <automated>cd /c/Users/adria/Desktop/pxts && python -c "exec(open('tests/_verify_07_02.py').read())"</automated>
  </verify>
  <done>
    - tests/_verify_07_02.py exists and runs without assertion errors
    - tsplot(backend="plotly") figure has rangeselector with 6 buttons: 1M, 3M, 6M, YTD, 1Y, All
    - rangeslider.visible is True by default
    - rangeslider=False makes rangeslider.visible == False
    - theme="dark" sets paper_bgcolor to "#1a1a2e"
    - matplotlib backend accepts new params and returns matplotlib.figure.Figure
    - All existing tests pass: pytest tests/ -x -q
  </done>
</task>

<task type="auto">
  <name>Task 2: Range nav and axis labels in _plot_ts_dual_plotly, updated tsplot_dual signature</name>
  <files>src/pxts/plots.py</files>
  <action>
0. Create the verify script at tests/_verify_07_02_dual.py with the following content (the file
   MUST exist before the verify command runs):

```python
# tests/_verify_07_02_dual.py
import pandas as pd
import matplotlib
matplotlib.use("Agg")
from pxts.plots import tsplot_dual, LEFT_COLOR, RIGHT_COLOR

idx = pd.date_range("2024-01-01", periods=5, freq="D")
df = pd.DataFrame({"A": [1.0, 2.0, 3.0, 2.5, 1.5], "B": [5.0, 4.0, 3.0, 4.5, 5.5]}, index=idx)

# Range selector on dual
fig = tsplot_dual(df, left=["A"], right=["B"], backend="plotly")
rs = fig.layout.xaxis.rangeselector
assert rs is not None, "dual: rangeselector missing"
assert len(rs.buttons) == 6, f"expected 6 buttons, got {len(rs.buttons)}"

# Rangeslider default on
assert fig.layout.xaxis.rangeslider.visible is True, "dual: rangeslider should be visible"

# Rangeslider opt-out
fig2 = tsplot_dual(df, left=["A"], right=["B"], rangeslider=False, backend="plotly")
assert fig2.layout.xaxis.rangeslider.visible is False

# left_label
fig3 = tsplot_dual(df, left=["A"], right=["B"], left_label="Energy", backend="plotly")
assert fig3.layout.yaxis.title.text == "Energy", f"left label: {fig3.layout.yaxis.title.text!r}"
assert fig3.layout.yaxis.title.font.color == LEFT_COLOR, (
    f"left label color: {fig3.layout.yaxis.title.font.color!r}"
)

# right_label
fig4 = tsplot_dual(df, left=["A"], right=["B"], right_label="Steam", backend="plotly")
assert fig4.layout.yaxis2.title.text == "Steam", f"right label: {fig4.layout.yaxis2.title.text!r}"
assert fig4.layout.yaxis2.title.font.color == RIGHT_COLOR, (
    f"right label color: {fig4.layout.yaxis2.title.font.color!r}"
)

# Matplotlib no error
import matplotlib.figure
fig5 = tsplot_dual(df, left=["A"], right=["B"], left_label="LHS", right_label="RHS",
                   backend="matplotlib")
assert isinstance(fig5, matplotlib.figure.Figure)
import matplotlib.pyplot as plt
plt.close(fig5)

print("_verify_07_02_dual OK")
```

1. Update tsplot_dual() public signature — add five params before backend=None:
       annotations=None, rangeslider: bool = True, theme: str = "light",
       left_label: str = None, right_label: str = None

   Add to docstring Args:
       annotations: list of annotation dicts. "col" key required to specify which
           y-axis drives y-lookup. Plotly backend only.
       rangeslider: show rangeslider. Default True. Plotly backend only.
       theme: "light" (default) or "dark". Plotly backend only.
       left_label: axis title text for the left (primary) y-axis, colored LEFT_COLOR.
           None (default) means no axis title.
       right_label: axis title text for the right (secondary) y-axis, colored RIGHT_COLOR.
           None (default) means no axis title.

2. Update tsplot_dual() plotly dispatch call to forward new params (matplotlib call UNCHANGED):
       return _plot_ts_dual_plotly(..., annotations=annotations, rangeslider=rangeslider,
                                   theme=theme, left_label=left_label, right_label=right_label,
                                   **kwargs)

3. Update _plot_ts_dual_plotly signature to accept:
       annotations=None, rangeslider=True, theme="light", left_label=None, right_label=None

4. Add range selector and rangeslider in _plot_ts_dual_plotly. After the x-axis update block
   (the if/else for date_format that calls fig.update_xaxes), add range navigation:

       rangeselector_cfg = dict(
           buttons=list([
               dict(count=1,  label="1M",  step="month", stepmode="backward"),
               dict(count=3,  label="3M",  step="month", stepmode="backward"),
               dict(count=6,  label="6M",  step="month", stepmode="backward"),
               dict(count=1,  label="YTD", step="year",  stepmode="todate"),
               dict(count=1,  label="1Y",  step="year",  stepmode="backward"),
               dict(step="all", label="All"),
           ]),
           bgcolor="rgba(255,255,255,0.8)",
           activecolor="#0072B2",
           x=0, y=1.02, xanchor="left", yanchor="bottom",
       )
       fig.update_xaxes(rangeselector=rangeselector_cfg,
                        rangeslider=dict(visible=rangeslider))

5. Add colored axis title text after the existing update_yaxes calls for tickfont:

       if left_label:
           fig.update_yaxes(title_text=left_label,
                            title_font=dict(color=LEFT_COLOR),
                            secondary_y=False)
       if right_label:
           fig.update_yaxes(title_text=right_label,
                            title_font=dict(color=RIGHT_COLOR),
                            secondary_y=True)

6. Apply dark theme in _plot_ts_dual_plotly after the title/subtitle block:

       if theme == "dark":
           from pxts.theme import (DARK_BACKGROUND_COLOR, DARK_PLOT_COLOR,
                                    DARK_GRID_COLOR, DARK_FONT_COLOR)
           fig.update_layout(paper_bgcolor=DARK_BACKGROUND_COLOR,
                             plot_bgcolor=DARK_PLOT_COLOR,
                             font=dict(color=DARK_FONT_COLOR))
           fig.update_xaxes(gridcolor=DARK_GRID_COLOR)
           fig.update_yaxes(gridcolor=DARK_GRID_COLOR)

The annotations parameter is accepted but is a no-op here. Plan 03 adds processing.
  </action>
  <verify>
    <automated>cd /c/Users/adria/Desktop/pxts && python -c "exec(open('tests/_verify_07_02_dual.py').read())"</automated>
  </verify>
  <done>
    - tests/_verify_07_02_dual.py exists and runs without assertion errors
    - tsplot_dual(backend="plotly") figure has rangeselector with 6 buttons
    - rangeslider defaults to visible; rangeslider=False hides it
    - left_label="Energy (kWh)" sets yaxis title_text with color matching LEFT_COLOR
    - right_label="Steam (kg/h)" sets yaxis2 title_text with color matching RIGHT_COLOR
    - theme="dark" applies dark colors
    - matplotlib backend accepts new params and returns Figure unchanged
    - All existing tests pass: pytest tests/ -x -q
  </done>
</task>

</tasks>

<verification>
Verify scripts are created during task execution (step 0 of each task action). Run them:

    cd /c/Users/adria/Desktop/pxts
    python -c "exec(open('tests/_verify_07_02.py').read())"
    python -c "exec(open('tests/_verify_07_02_dual.py').read())"

Full regression: pytest tests/ -x -q
</verification>

<success_criteria>
- tsplot and tsplot_dual Plotly figures have 6 range selector buttons (1M/3M/6M/YTD/1Y/All) by default
- rangeslider=False suppresses the slider on both renderers
- theme="dark" produces dark navy background on both renderers
- tsplot_dual left_label/right_label produce colored axis title text
- Matplotlib backend ignores new params and returns matplotlib.figure.Figure unchanged
- All 56 existing tests pass
</success_criteria>

<output>
After completion, create .planning/phases/07-interactive-plotly-time-series-charts/07-02-SUMMARY.md
</output>
