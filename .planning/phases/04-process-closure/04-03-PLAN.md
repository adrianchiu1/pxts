---
phase: 04-process-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pxts/plots.py
  - tests/conftest.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "plots.py _detect_plotly_tickformat no longer contains 'import numpy as np'"
    - "conftest.py no longer defines the tmp_csv fixture"
    - "All 109 tests still pass after both removals"
  artifacts:
    - path: "src/pxts/plots.py"
      provides: "plots.py without dead numpy import inside _detect_plotly_tickformat"
      not_contains: "import numpy as np"
    - path: "tests/conftest.py"
      provides: "conftest.py without unused tmp_csv fixture"
      not_contains: "tmp_csv"
  key_links:
    - from: "src/pxts/plots.py"
      to: "_detect_plotly_tickformat"
      via: "function body"
      pattern: "def _detect_plotly_tickformat"
---

<objective>
Remove two pieces of dead code identified by the v0.1 audit: a dead numpy import inside _detect_plotly_tickformat and an unused tmp_csv fixture in conftest.py.

Purpose: The audit flagged a latent ImportError risk from `import numpy as np` inside _detect_plotly_tickformat (numpy is never called — pd.Series.median() is used instead, numpy is not declared as a dependency). Also flagged tmp_csv fixture in conftest.py as defined but never used by any test function.
Output: Cleaner plots.py and conftest.py with no regressions.
</objective>

<execution_context>
@C:/Users/adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead numpy import from _detect_plotly_tickformat in plots.py</name>
  <files>src/pxts/plots.py</files>
  <action>
Read src/pxts/plots.py and find _detect_plotly_tickformat. Inside the function body there is a line:
```python
import numpy as np
```
Remove this line. numpy is not used anywhere in this function — the median is computed via `pd.Series.median()`. Removing this import eliminates a latent ImportError if numpy is absent from the environment (numpy is not listed in pyproject.toml dependencies).

Do NOT change any other code in the function. Do NOT remove numpy from anywhere else in the file if it is used there.
  </action>
  <verify>
    <automated>python -c "
src = open('src/pxts/plots.py').read()
# Find _detect_plotly_tickformat function
idx = src.find('def _detect_plotly_tickformat')
assert idx != -1, 'function not found'
# Get function body (until next def at same indent level)
func_end = src.find('\ndef ', idx + 1)
func_body = src[idx:func_end] if func_end != -1 else src[idx:]
assert 'import numpy' not in func_body, 'numpy import still present in function'
print('PASS: dead numpy import removed')
"</automated>
  </verify>
  <done>import numpy as np removed from inside _detect_plotly_tickformat. No other changes to plots.py.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused tmp_csv fixture from conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
Read tests/conftest.py and find the tmp_csv fixture. It is defined but never used by any test function — all IO tests create their own inline CSVs via tmp_path directly.

Remove the entire tmp_csv fixture definition (the @pytest.fixture decorator line and the function body). Also remove any import that was used exclusively by tmp_csv (e.g., pathlib.Path if it is only used there — but check first).

Do NOT change any other fixture (ts_df, bad_df).
  </action>
  <verify>
    <automated>python -c "
src = open('tests/conftest.py').read()
assert 'tmp_csv' not in src, 'tmp_csv still in conftest.py'
print('PASS: tmp_csv fixture removed')
"</automated>
  </verify>
  <done>tmp_csv fixture removed from conftest.py. ts_df and bad_df fixtures unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
Run:
```bash
pytest tests/ -q --tb=short
```
All 109 tests must pass. If any test imports or uses tmp_csv, the test would fail and that test must also be fixed (but per the audit, no test uses tmp_csv — this is a safety check).
  </action>
  <verify>
    <automated>pytest tests/ -q --tb=short 2>&1 | tail -3</automated>
  </verify>
  <done>pytest exits 0, all tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Confirm numpy import removed
python -c "
src = open('src/pxts/plots.py').read()
idx = src.find('def _detect_plotly_tickformat')
func_end = src.find('\ndef ', idx + 1)
func_body = src[idx:func_end]
assert 'import numpy' not in func_body
print('numpy: PASS')
"

# Confirm tmp_csv removed
python -c "
src = open('tests/conftest.py').read()
assert 'tmp_csv' not in src
print('tmp_csv: PASS')
"

# Confirm tests pass
pytest tests/ -q --tb=no
```
</verification>

<success_criteria>
- `import numpy as np` removed from inside _detect_plotly_tickformat in plots.py
- tmp_csv fixture removed from tests/conftest.py
- All 109 tests still pass (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/04-process-closure/04-03-SUMMARY.md`
</output>
