---
phase: 02-bug-fixes-and-dependencies
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/pxts/io.py
autonomous: true
requirements:
  - DEP-01
  - FIX-01

must_haves:
  truths:
    - "A fresh install that pip-installs pxts will have cycler available without a separate install step"
    - "Loading a CSV with an ambiguous date like '01/02/2024' emits a UserWarning that names the assumed format and the override parameter"
    - "Loading a CSV with an explicit date_format param suppresses the warning entirely"
  artifacts:
    - "pyproject.toml has cycler in [project].dependencies alongside pandas"
    - "src/pxts/io.py _detect_date_format emits warnings.warn in the ambiguous branch"
  key_links:
    - "warnings.warn(..., UserWarning, stacklevel=2) in _detect_date_format — stacklevel must point to the read_ts call site, not the internal function"
    - "cycler declared in [project].dependencies (not [project.optional-dependencies])"
---

<objective>
Declare cycler as an explicit runtime dependency and make the ambiguous date-format path warn users instead of silently defaulting to US format.

Purpose: DEP-01 prevents import-time failures on theme application for users who didn't install extras. FIX-01 surfaces a silent data-corruption risk — when a date like '01/02/2024' is ambiguous, the user must know what format was assumed.
Output: Updated pyproject.toml; updated src/pxts/io.py with warning in the ambiguous slash-date branch.
</objective>

<execution_context>
@C:/Users/adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From src/pxts/io.py — the function to modify -->
<!-- _detect_date_format(sample: str) -> tuple[str, bool] -->
<!-- Returns (format_string, dayfirst). Ambiguous branch is at line 60-62: -->
<!--   else: -->
<!--       # Ambiguous — default to US MM/DD/YYYY -->
<!--       return ("%m/%d/%Y", False) -->
<!--                                                                           -->
<!-- read_ts() calls _detect_date_format at line 104: -->
<!--   detected_format, dayfirst = _detect_date_format(sample_str) -->
<!-- The short-circuit at line 96-99 (explicit date_format) bypasses _detect_date_format entirely — no warning needed there. -->
<!--                                                                           -->
<!-- warnings is already imported in core.py but NOT in io.py — must add import. -->
<!-- to_offset is NOT needed in io.py — only relevant to core.py. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cycler to runtime dependencies (DEP-01)</name>
  <files>pyproject.toml</files>
  <behavior>
    - After change: `[project].dependencies` contains both `"pandas>=2.0"` and `"cycler"` (no version pin required — any cycler compatible with matplotlib)
    - The `[project.optional-dependencies]` sections are unchanged
  </behavior>
  <action>
In pyproject.toml, update the `[project]` `dependencies` array to include `"cycler"`:

```toml
dependencies = ["pandas>=2.0", "cycler"]
```

cycler is already a transitive dependency of matplotlib but is used directly in theme.py — declaring it explicitly prevents breakage when matplotlib is not installed (base install path). No version pin needed.
  </action>
  <verify>
    <automated>python -c "import tomllib; data = tomllib.loads(open('pyproject.toml').read()); assert 'cycler' in data['project']['dependencies'], 'cycler missing from dependencies'; print('OK')"</automated>
  </verify>
  <done>pyproject.toml [project].dependencies contains "cycler"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warn on ambiguous slash-delimited date format (FIX-01)</name>
  <files>src/pxts/io.py</files>
  <behavior>
    - `_detect_date_format("01/02/2024")` emits a UserWarning containing the sample value, the assumed format string `MM/DD/YYYY`, and the override hint `date_format=`
    - `_detect_date_format("13/02/2024")` does NOT warn (unambiguous UK format, first part > 12)
    - `_detect_date_format("01/13/2024")` does NOT warn (unambiguous US format, second part > 12)
    - `_detect_date_format("2024-01-02")` does NOT warn (ISO format)
    - The existing test `test_ambiguous_slash_csv_treated_as_us` must still pass (behavior unchanged — format is still US, just now with a warning)
    - `read_ts(path, date_format='%d/%m/%Y')` suppresses the warning entirely (explicit format bypasses _detect_date_format)
  </behavior>
  <action>
In src/pxts/io.py:

1. Add `import warnings` at the top of the file (after `import re`).

2. In `_detect_date_format`, replace the ambiguous branch (the `else` block at the bottom of the slash_match block) with:

```python
else:
    # Ambiguous — both parts <= 12 — default to US MM/DD/YYYY
    warnings.warn(
        f"pxts: ambiguous date '{sample}' — assumed MM/DD/YYYY (US). "
        f"Pass date_format='%d/%m/%Y' to override.",
        UserWarning,
        stacklevel=3,  # _detect_date_format -> read_ts -> user call site
    )
    return ("%m/%d/%Y", False)
```

stacklevel=3 because the call chain is: user code -> read_ts -> _detect_date_format. This points the warning at the user's read_ts() call site.

The returned value is unchanged — only the warning is added. The explicit `date_format` path in `read_ts` (lines 96-99) bypasses `_detect_date_format` entirely, so no warning fires when the user provides an explicit format.

The test `test_ambiguous_slash_csv_treated_as_us` asserts current US-default behavior — it should now be updated (or a new test added) to also assert the warning is emitted. Update the test with `pytest.warns(UserWarning, match="ambiguous date")`.
  </action>
  <verify>
    <automated>pytest tests/test_io.py -x -q</automated>
  </verify>
  <done>
    - `_detect_date_format` emits UserWarning for ambiguous slash dates (both parts ≤ 12)
    - Warning message contains the sample date, assumed format, and override hint
    - All existing test_io.py tests pass (including updated ambiguous test)
    - Unambiguous and ISO dates produce no warning
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions across modules:

```
pytest -x -q
```

All tests must pass. If the ambiguous-date test in test_io.py was asserting no warning, it needs updating to expect one — that is intentional behavior change, not a regression.
</verification>

<success_criteria>
1. `pyproject.toml` lists `"cycler"` in `[project].dependencies`
2. `_detect_date_format("01/02/2024")` emits a `UserWarning` naming the assumed format and override param
3. `pytest tests/test_io.py -x -q` passes with zero failures
4. `pytest -x -q` (full suite) passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-bug-fixes-and-dependencies/02-01-SUMMARY.md`
</output>
